import React, { FC } from 'react'
import styles from './index.less'

interface RankCardProps {
  data: any[]
}

const RankCard: FC<RankCardProps> = props => {
  return <div>RankCard</div>
}

export default RankCard
